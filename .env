# Same-origin path to the bridge API. In production HouseGraph reverse-proxies
# /bridge -> the AC bridge; in dev, vite.config.ts proxies it to localhost:3000.
# No CORS, no bridge.local resolution needed by client devices.
VITE_BRIDGE_URL=/bridge
